install Windows support on Packer/Ansible controller.

sudo pip install --prefix=/usr/local "pywinrm>=0.1.1" 

populate group_vars/windows.yml (ssl: http://docs.ansible.com/ansible/intro_windows.html)
Packer has {{RMI_PASSWORD}} or something like that

ansible_user: administrator
ansible_password: <password>
ansible_port: 5985
ansible_connection: winrm
ansible_winrm_scheme: http
ansible_winrm_server_cert_validation: ignore


Fix BigFix - 
* need non-interactive. setup.exe /s /v"/l*voicewarmup \"C:\besclientinstall.log\" SETUPEXE=1 /qn”
* if connection params provided during image build, needs to be removed! (https://www.ibm.com/support/knowledgecenter/SSQL82_9.5.0/com.ibm.bigfix.doc/Platform/Installation/c_for_windows.html)

aws-inspector.install.args needs a '+log' to tack on /log parameter
